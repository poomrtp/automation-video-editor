# video-autocut dependencies
# ติดตั้งด้วย: pip install -r requirements.txt

ffmpeg-python>=0.2.0

# หมายเหตุ:
# - ต้องติดตั้ง ffmpeg ใน system ด้วย (ไม่ใช่ Python package)
#   macOS:   brew install ffmpeg
#   Windows: choco install ffmpeg  หรือ  winget install ffmpeg
#
# - ต้องติดตั้ง Claude Code CLI:
#   curl -fsSL https://claude.ai/install.sh | bash   (macOS/Linux)
#   https://claude.ai/download                        (Windows)
#
# - login Claude CLI ด้วย Pro account:
#   claude login
